# API URL for backend (default: proxied via next.config.js)
# NEXT_PUBLIC_API_URL=http://localhost:5000

# Canonical site URL for SEO (metadata, Open Graph, sitemaps). No trailing slash.
# NEXT_PUBLIC_SITE_URL=https://baws.example.com
